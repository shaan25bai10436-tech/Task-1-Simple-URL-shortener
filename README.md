# Task-1-Simple-URL-shortener
Create a backend server using Flask (Python) or Express.js (Node.js).  ● Build an API endpoint to accept long URLs and generate a unique short code.  ● Store the mapping of short code and original URL in a database (like SQLite, MongoDB, etc.).  ● Set up a redirect route where accessing the short URL takes the user to the original long URL. 
